{
  "name": "smell-away-health-game",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "npm run build && node scripts/serve-dist.mjs",
    "build": "node scripts/build-dist.mjs",
    "postbuild": "node scripts/copy-optimized-assets.mjs",
    "preview": "node scripts/serve-dist.mjs",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "vite": "^5.4.11",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "tailwindcss": "^3.4.17",
    "postcss": "^8.4.49",
    "autoprefixer": "^10.4.20"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
